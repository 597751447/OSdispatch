#include <iostream>
#include "common/utils.h"
#include "solve/spf.h"

int main() {
    LinkList linkList;
    Create(linkList);
    Show(linkList);
    Sort(linkList);
    //Srtf(linkList);
    //SPF(linkList);
    return 0;
}
